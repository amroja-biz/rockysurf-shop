# index.json is generated — `npx rockysurf pack index --source packs --out index.json` — and a
# pull request must carry its own regeneration, which CI verifies against packs/. Ownership here
# is a second pair of eyes on a file every contributor now touches, not a prohibition.
/index.json @jbdamask
/.github/   @jbdamask
